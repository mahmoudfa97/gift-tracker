'use client'

import GiftTracker from "../components/ui/GiftTracker"

export default function GiftTrackerPage() {
  return (
    <main className="min-h-screen bg-background flex flex-col items-center p-4">
      <GiftTracker />
    </main>
  )
}
